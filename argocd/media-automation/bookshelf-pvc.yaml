apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookshelf-pvc
  namespace: plex
  labels:
    app: bookshelf
spec:
  storageClassName: ceph-block-ssd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
