apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pulsarr-pvc
  namespace: plex
  labels:
    app: pulsarr
spec:
  storageClassName: ceph-block-ssd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

